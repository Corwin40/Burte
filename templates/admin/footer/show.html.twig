{% extends 'base.html.twig' %}

{% block title %}Footer{% endblock %}

{% block body %}
    <h1>Footer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ footer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ footer.name }}</td>
            </tr>
            <tr>
                <th>TypeContent</th>
                <td>{{ footer.typeContent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_footer_index') }}">back to list</a>

    <a href="{{ path('admin_footer_edit', {'id': footer.id}) }}">edit</a>

    {{ include('admin_footer/_delete_form.html.twig') }}
{% endblock %}
